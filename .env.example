# Local secrets — DO NOT COMMIT (see .gitignore)
#
# Setup:
#   1. Copy this file:  copy .env.example .env
#   2. Paste your key below (free key: https://aistudio.google.com/apikey)
#   3. gw ask is optional — all other commands work without a key

GEMINI_API_KEY=
